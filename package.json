{
  "name": "edict",
  "version": "0.1.0",
  "description": "Edict — An agent-first programming language. AST-first compiler pipeline.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "generate-schema": "tsx scripts/generate-schema.ts"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "typescript-json-schema": "^0.65.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "binaryen": "^125.0.0",
    "z3-solver": "^4.15.8"
  }
}
